export default function App() {
  return (
    <div className="shell">
      <header className="topbar">
        <span className="wordmark">AIVA</span>
        <span className="surface-label">Recruiter Console</span>
      </header>
      <main className="content">
        <section className="empty-state" aria-labelledby="empty-heading">
          <h1 id="empty-heading">Pipeline arrives in Milestone 5</h1>
          <p>
            This console lists requisitions and candidates once resume ingest and
            scoring land. The foundation — infrastructure, air-gap enforcement, and
            the design system — is being verified first.
          </p>
        </section>
      </main>
    </div>
  );
}
